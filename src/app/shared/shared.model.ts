export type SortDir = 'asc' | 'desc';
export type StockStatus = 'In Stock' | 'Out Of Stock';
export type ToasterMessage = {
  summary: string;
  detail: string;
};
export type ConfirmationBoxTranslation = {
  title: string;
  message: string;
  confirmBtn: string;
  cancelBtn: string;
};
